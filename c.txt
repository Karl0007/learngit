变量类型 {bool,char,int,long long,float,double,long double}

变量名

变量生命周期

变量赋初值

变量类型转换(低精度->高精度 整型->浮点型)

类型强制转换

常量

数组

指针

函数

递归

表达式

优先级顺序
() [] ->
! ~ ++ --
* / %
+ -
<< >>
> >= < <=
== !=
&
^
|
&&
||
?:
= += -=...
,

i++ 与 ++i

避免未定义表达式

浮点数的比较

''字符 ""字符串
'\'转义符

if-else 匹配问题 加{}

switch(整型){
	case : break;
	case : break;
	default: break;
}

while(){
	
}

for (;;){
	
}

break;
continue;
尽量少用goto 

排序
二分查找
链表


输入一行字符，分别统计出其中英文字母,空格,数字和其它字符的个数。并把小写转换为大写，大写转换为小写

判断一个数字是不是回文素数

输出1-n中所有的完数 完数的定义：除自身外的因子之和等于它本身。

 输入某一个自然数n，设其反序数为m，令N=n+m，得到一个新的自然数N。再计算该自然数N的反序数为M，令NN=N+M，得到一个新的自然数NN。问：重复上述操作多少次，使得自然数n变成一个回文数，并输出该回文数。
提示1：247的反序数是742，两者之和989是回文数，此时执行了1次操作；
提示2：符号*用于间隔输出。 
694 3*3113

统计字符串中某字符出现的位置和次数 并且输出删除这些字符后的字符串

要求找出具有下列性质数的个数（包含输入的自然数n）： 
先输入一个自然数n(n<=1000),然后对此自然数按照如下方法进行处理: 
1. 不作任何处理; 
2. 在它的左边加上一个自然数,但该自然数不能超过原数的一半; 
3. 加上数后,继续按此规则进行处理,直到不能再加自然数为止.